.sidebar {
  padding-top: 25px;

  &__block {
    margin-bottom: 25px;
    padding: 15px 25px;
    border: 1px solid rgba(100, 100, 100, 0.19);
    border-radius: 3px;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
    background: #fff;
    color: #616161;
    font-size: 12px;
  }
}